ErtenApp PWA icons

The actual PNG icon files referenced in `manifest.webmanifest` are:

- /icons/icon-192.png
- /icons/icon-512.png
- /icons/maskable-icon-192.png
- /icons/maskable-icon-512.png

You can replace them with your own assets keeping the same filenames and sizes.

